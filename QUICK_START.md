# Быстрый старт: Публикация на GitHub

## Шаг 1: Подготовка репозитория

1. Создайте репозиторий на GitHub (если еще не создан)
2. Клонируйте репозиторий локально
3. Скопируйте все файлы проекта в репозиторий

## Шаг 2: Настройка файлов

### Обновите ссылки в `docs/index.html`:

Откройте `docs/index.html` и замените:
- `YOUR_USERNAME` на ваш GitHub username
- `YOUR_REPOSITORY` на название вашего репозитория

Найдите в файле строку:
```javascript
const GITHUB_USERNAME = 'YOUR_USERNAME';
const GITHUB_REPOSITORY = 'YOUR_REPOSITORY';
```

И замените на:
```javascript
const GITHUB_USERNAME = 'ваш_username';
const GITHUB_REPOSITORY = 'ваш_репозиторий';
```

### Обновите ссылки в `RELEASE_NOTES.md`:

Замените все вхождения:
- `YOUR_USERNAME` → ваш GitHub username
- `YOUR_REPOSITORY` → название репозитория

### Обновите ссылки в `README.md`:

Замените:
- `YOUR_USERNAME` → ваш GitHub username
- `YOUR_REPOSITORY` → название репозитория

## Шаг 3: Первый релиз

### 3.1 Обновите версию (если нужно):

```powershell
.\update-version.ps1 -NewVersion "1.0.0"
```

### 3.2 Соберите приложение:

```powershell
.\build.ps1
.\build-installer.ps1
```

### 3.3 Создайте релиз на GitHub:

1. Перейдите на https://github.com/YOUR_USERNAME/YOUR_REPOSITORY/releases/new
2. Заполните:
   - **Tag version:** `v1.0.0`
   - **Release title:** `ChatList v1.0.0`
   - **Description:** Скопируйте из `RELEASE_NOTES.md`
3. Загрузите файлы:
   - `installer\ChatList-Setup-v1.0.0.exe`
   - (Опционально) `dist\ChatList-v1.0.0.exe`
4. Нажмите "Publish release"

## Шаг 4: Настройка GitHub Pages

1. Перейдите в Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` (или `gh-pages`)
4. Folder: `/docs`
5. Save

Через несколько минут ваш сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/YOUR_REPOSITORY/`

## Шаг 5: Автоматизация (опционально)

Для автоматических релизов при создании тега:

1. Убедитесь, что файл `.github/workflows/release.yml` существует
2. Создайте тег:
   ```powershell
   git tag v1.0.0
   git push origin v1.0.0
   ```
3. GitHub Actions автоматически соберет и опубликует релиз

## Готово! 🎉

Теперь у вас есть:
- ✅ Репозиторий на GitHub
- ✅ Релизы с установщиками
- ✅ Лендинг на GitHub Pages
- ✅ Автоматизация через GitHub Actions

## Полезные команды

```powershell
# Обновить версию
.\update-version.ps1 -NewVersion "1.0.1"

# Собрать приложение
.\build.ps1
.\build-installer.ps1

# Создать тег для автоматического релиза
git tag v1.0.1
git push origin v1.0.1
```
